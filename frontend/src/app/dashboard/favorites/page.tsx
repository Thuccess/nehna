import FavoritesView from '@/features/favorites/components/FavoritesView';

export const metadata = {
  title: 'Favorites — NehnaX Dashboard',
};

export default function DashboardFavoritesPage() {
  return <FavoritesView />;
}
