# Roadmap (post-structure)

Deferred until product scale requires them:

- `Category`, `Location`, `Ad` Mongoose models + admin ads routes
- Background `jobs/` (email, image optimization, approval workflows)
- `analytics.service.ts` + `app/admin/analytics/`
- `features/ads/` on the frontend
- pnpm + Turborepo (optional team scaling)
- Client `store/` (only if React Query + context is insufficient)
